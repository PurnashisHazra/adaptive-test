PORT=8787
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>/<dbname>?retryWrites=true&w=majority
MONGO_DB_NAME=adaptest
MONGO_COLLECTION_NAME=website_leads
# Comma-separated browser origins allowed to call this API (e.g. your static site). Leave unset to allow any origin (local dev only).
# CORS_ORIGINS=https://your-marketing-site.vercel.app,https://www.example.com
